machine:
  node:
    version: 7
dependencies:
  pre:
    - curl -o- -L https://yarnpkg.com/install.sh | bash
  override:
    - cd client && yarn
test:
  override:
    - cd client && npm run build
deployment:
  github-pages:
    branch: master
    commands:
      - cd client && npm run deploy
